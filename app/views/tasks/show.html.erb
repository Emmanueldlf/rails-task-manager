<div class="container">
<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @completed %></p>
<p><%= link_to 'Index', tasks_path %></p>
<p><%= link_to 'Edit', edit_task_path(@task) %></p>
<p><%= link_to 'Destroy', task_path(@task), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{@task.title}?" } %></p>


</div>
